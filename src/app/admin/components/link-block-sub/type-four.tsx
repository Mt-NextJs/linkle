import { twMerge } from "tailwind-merge";

interface LinkBlockProps {
  url: string;
  style: number | null;
  imgUrl: string | null;
  title: string | null;
}

export default function TypeFour({
  url,
  style,
  imgUrl,
  title,
}: LinkBlockProps) {
  return <></>;
}
