interface DivideBlockProps {
  type: number;
  sequence: number;
  style: number | null;
}

export default function DivideBlock({
  type,
  sequence,
  style,
}: DivideBlockProps) {
  return <></>;
}
