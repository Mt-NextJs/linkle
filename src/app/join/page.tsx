import { Metadata } from 'next';

//metadata
export const metadata: Metadata = {
    title: 'Join',
};

export default function Join() {
    return (
        <div>
            <h1>회원가입 페이지</h1>
        </div>
    );
}
